<%= devise_error_messages! %>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {class: 'login-form'}) do |f| %>
    <div class="login-wrap">

      <p class="login-img"><i class="icon_lock_alt"></i></p>
      <span>Логин:</span>
      <div class="input-group">
        <span class="input-group-addon"><i class="icon_profile"></i></span>
        <%= f.text_field :name, autofocus: true, class: 'form-control' %>
      </div>
      <span>Почта:</span>
      <div class="input-group">
        <span class="input-group-addon"><i class="icon_mail"></i></span>
        <%= f.text_field :email, class: 'form-control' %>
      </div>
      <% if @minimum_password_length %>
          <span>Пароль: </span><em>(<%= @minimum_password_length %> минимум символов)</em>
      <% end %><br/>
      <div class="input-group">
        <span class="input-group-addon"><i class="icon_key_alt"></i></span>
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
      </div>
      <span>Повторный пароль:</span>
      <div class="input-group">
        <span class="input-group-addon"><i class="icon_key_alt"></i></span>
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
      </div>

      <%= f.submit "Регистрация", class: 'btn btn-primary btn-lg btn-block' %>

      <%= link_to new_rootadmin_session_path, class: 'btn btn-info btn-lg btn-block rootReg' do %>
          <span>Войти</span>
      <% end %>

    </div>

<% end %>


