<%= form_for(twocategory, :html => {:class => 'form-horizontal', multipart: true}) do |f| %>
  <% if twocategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twocategory.errors.count, "error") %> prohibited this twocategory from being saved:</h2>

      <ul>
      <% twocategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
<div class="form-group col-md-offset-2">
  <div class="col-sm-2 ">
    <%= f.label 'Основная картинка', class:"control-label" %>
    </div>
  <div class="col-sm-10 ">
    <ul class="sliderImg imgcat">
    <li><%= image_tag @twocategory.image.url, title: "#{@twocategory.name}", class: "EditImage" %></li>
     <li><%= f.file_field :image %><% unless @twocategory.image_file_name.nil? %></li>
    <li><%= link_to("Удалить", {action: :delete_attachment, id: @twocategory.id}, method: :put,data: { confirm: 'Вы уверины что хотите удалить?' }) %></li>
<% end %>
</ul> 
  </div>
</div>

    <div class="form-group">
      <div class="col-sm-2 ">
        <%= f.label 'Название', class: "control-label" %>
      </div>
      <div class="col-sm-10 ">
        <%= f.text_field :name, class: "form-control form" %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-2 ">
        <%= f.label 'Описание', class: "control-label" %>
      </div>
      <div class="col-sm-10 ">
        <%= f.text_area :description, class: "form-control form" %>
      </div>
    </div>

   <div class="form-group">
     <div class=" col-sm-2">
       <%= f.label 'Выберите категорию', class:"control-label" %>
     </div>
     <div class="col-sm-10 ">
       <%= f.select( "category_id", Category.all.collect {|category| [ category.name, category.id ] }, { :include_blank => '- Категории -' }, :class => 'form-control') %>
     </div>


</div>
 <%= f.submit "Сохранить", :class => 'btn btn2 actions col-md-offset-4' %>
    <%= link_to 'Назад', admin_panel_twocategories_path, class: 'btn btn-primary' %>
<% end %>


